apiVersion: serving.knative.dev/v1
kind: Service
metadata:
  name: pubsub-client
spec:
  template:
    spec:
      containers:
      - image: gcr.io/rikudog/pubsub-client:v1
        imagePullPolicy: Always
